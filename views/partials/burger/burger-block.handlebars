<li>
    {{id}}
    {{burger_name}}
    {{#if devoured}}
    {{else}}
    <button class="change-burger" data-id="{{id}}" data-newDevoured="{{Devoured}}">
        {{#if devoured}}{{else}}DEVOUR!{{/if}}
    </button>
    {{/if}}
</li>